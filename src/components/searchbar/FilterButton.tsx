import { Faders } from 'phosphor-react';
export const FilterButton = () => {
  return (
    <button className="px-2 py-2 border rounded-sm">
      <Faders size={25} weight="light" />
    </button>
  );
};
